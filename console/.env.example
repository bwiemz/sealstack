# Gateway base URL the console talks to. Override for remote deployments.
PUBLIC_GATEWAY_URL=http://localhost:7070

# Default caller identity sent in `X-Cfg-User`. Production deployments swap
# this for OAuth/JWT at the edge; v0.1 accepts this as an honor-system header.
PUBLIC_DEFAULT_USER=console
