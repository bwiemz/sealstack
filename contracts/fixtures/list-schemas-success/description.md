# list-schemas-success

`GET /v1/schemas` returns every registered schema as a list of
`SchemaMetaWire` projections. Tests the array-shaped happy path of
the admin namespace and confirms the SDK list parsing.
