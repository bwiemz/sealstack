# list-connectors-success

`GET /v1/connectors` returns every registered connector binding as
`ConnectorBindingWire` projections. Tests the list-namespace happy
path and the SDK's array-decoding path.
