# Architecture

Five layers: ingestion, memory, retrieval, policy, and surface. See the
root plan doc for details on each.

The gateway is the product surface. The console is a replaceable reference UI.
