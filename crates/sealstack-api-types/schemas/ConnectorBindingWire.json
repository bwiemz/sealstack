{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://contracts.sealstack.dev/api-types/v0.3.0/ConnectorBindingWire.json",
  "title": "ConnectorBindingWire",
  "description": "Public projection of `sealstack_ingest::ConnectorBindingInfo` for the REST surface. Intentionally diverges in two places: renames `connector` → `kind` for SDK ergonomics, and adds an `enabled: bool` that the gateway computes from the binding's runtime state (no engine field to mirror directly). The gateway maps fields at the response boundary.",
  "type": "object",
  "required": [
    "enabled",
    "id",
    "kind",
    "schema"
  ],
  "properties": {
    "id": {
      "description": "Binding ID.",
      "type": "string"
    },
    "kind": {
      "description": "Connector kind.",
      "type": "string"
    },
    "schema": {
      "description": "Qualified schema name.",
      "type": "string"
    },
    "enabled": {
      "description": "Whether the binding is enabled.",
      "type": "boolean"
    }
  }
}
