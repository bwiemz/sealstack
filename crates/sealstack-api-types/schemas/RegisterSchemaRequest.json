{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://contracts.sealstack.dev/api-types/v0.3.0/RegisterSchemaRequest.json",
  "title": "RegisterSchemaRequest",
  "description": "Request body for `POST /v1/schemas`.",
  "type": "object",
  "required": [
    "meta"
  ],
  "properties": {
    "meta": {
      "description": "Schema metadata as emitted by `sealstack_csl::codegen`. Free-shaped here to avoid coupling api-types to the CSL crate; gateway parses into the typed `sealstack_engine::SchemaMeta` internally."
    }
  }
}
